# Secrets — never commit
.env
*.json.local

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Vector store (binary — rebuild via ingestion script instead)
chroma_db/

# Persistent memory DB (local — rebuilt/seeded automatically on startup)
abl_memory.db
*.db

# OS / editor
.DS_Store
.vscode/
.idea/

# Streamlit local secrets
.streamlit/secrets.toml
